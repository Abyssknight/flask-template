version: '3'

services:
  web:
    build: .
    ports:
      - '5000:5000'
    command: gunicorn -c gunicorn.py wsgi:app
  cron:
    build: .
    command: flask cron
  celery:
    build: .
    environment:
      - C_FORCE_ROOT=1
    command: flask celery worker
